abstract class BrussType {
  // String endpoint();

  const BrussType();
}

// T request<T extends Data>(T Function(String) construct) {
//   // make request
//   const String response = '{"endpoint": "a", "attribute": 0}';
//   return construct(response);
// }
